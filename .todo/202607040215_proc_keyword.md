# Archived proc Keyword Support (2026-07-04)

Previous Commit ID: e7fa526

All tasks to support the `proc` keyword and default safe `fn` declarations have been completed:

## Completed Tasks

### proc Keyword Parser and Lexer
- [x] Add `Proc` variant to `Token` enum in `src/lexer.rs`.
- [x] Map `"proc"` string literal to `Token::Proc` in lexer.
- [x] Match `Token::Proc` in top-level item parser in `src/parser.rs`.
- [x] Modify `parse_fn` to consume either `Token::Fn` or `Token::Proc`, setting `is_unsafe = true` if `Token::Proc` is parsed.

### transform and Emitter updates
- [x] Modify `transform_fn` in `src/transform.rs` to only wrap the function body in `unsafe {}` if `func.is_unsafe` is true.
- [x] Verify signature emitter in `src/emitter.rs` generates correct `pub unsafe extern "C" fn` ordering for unsafe functions.

### Fixtures and Tests updates
- [x] Convert FFI and pointer-oriented fixtures (`libc_types.carbide`, `apr_types.carbide`, `ffi_compute.carbide`) from `fn` to `proc` declarations.
- [x] Keep native safe types test (`rust_primitives.carbide`) as `fn` declarations.
- [x] Update unit, fixture, and integration tests to assert correct output signatures and `unsafe` blocks.
